[tool:pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --html=report.html
    --self-contained-html
    --cov=../src
    --cov-report=html
    --cov-report=term-missing